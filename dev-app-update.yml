# 开发环境（未打包 Windows）手动检查更新时使用的更新源配置。
# 由 electron-updater 的 forceDevUpdateConfig 读取；不随安装包分发（build.files 未包含本文件）。
# 正式打包环境的更新源以 package.json 的 build.publish 为准，两者必须保持一致。
provider: github
owner: dunhanson
repo: apex-ssh
